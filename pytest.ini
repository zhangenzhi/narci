[pytest]
# 测试发现范围(P4.5):只收真正的测试目录,排除 tools/ 与 research/ 里
# 以 test_ 命名但实为探针/一次性脚本的文件(如 tools/test_um_aggtrade_ws.py
# 需要 live WS)。新模块测试请放到 tests/<module>/ 下。
testpaths =
    tests
    analytics/calibration/tests
