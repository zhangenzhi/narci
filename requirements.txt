pandas>=2.0
pyarrow>=14.0
numpy>=1.24
aiohttp>=3.9
websockets>=12.0
# socket.io v2 (EIO=3) for bitbank — Binance/CC 不需要,装上不影响它们。
python-socketio[asyncio_client]>=5.0
pyyaml>=6.0
requests>=2.31
supervisor>=4.2
